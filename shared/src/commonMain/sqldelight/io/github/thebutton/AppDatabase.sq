CREATE TABLE Database (
    id INTEGER PRIMARY KEY,
    count INTEGER
);

removeAllCounts:
DELETE FROM Database;

selectAll:
SELECT Database.*
FROM Database;

replaceCount:
REPLACE INTO Database(id, count) VALUES(?, ?);

selectCount:
SELECT count
FROM Database
WHERE id = ?;
